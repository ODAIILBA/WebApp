# Client-side static JavaScript — innerHTML usage is in controlled contexts
# where only static HTML structures and server-validated data are rendered
public/static/

# Migration SQL files — bcrypt hashes are hashed admin passwords stored
# in the DB schema seed, not plaintext secrets
migrations/

# Development utility script — child_process is intentional for running
# wrangler CLI commands during local DB migration
apply-migrations.js

# Build output — not source code
dist/
